import React, { Component } from 'react'; // Object destructuring

class Header extends Component { // classes mass produce objects
    render() {
        return (
            <h1>React Actors</h1>
        );
    }
}

export default Header;
